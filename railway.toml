# Railway config — overrides dashboard start command on deploy
# https://docs.railway.app/guides/config-as-code

[build]
builder = "nixpacks"
buildCommand = "npm run build:admin"

[deploy]
startCommand = "npm start"
