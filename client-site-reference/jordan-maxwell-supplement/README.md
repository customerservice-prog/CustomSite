Reference pages for projects that link **biography.html**, **research.html**, and **about.html** from `index.html` nav but are missing those files in `site_files`.

Upload each file to the project root with **PUT** `/api/admin/projects/:id/site/file` (same paths as your nav). Adjust `<link href="styles.css">` if your CSS filename differs.
